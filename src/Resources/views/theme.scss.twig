/*
------------------------------------------------------------
Customization
------------------------------------------------------------
*/
$white:                             {{ (white.0|length>0)?'#'~white.0:'#fff' }};
$gray-100:                          {{ (gray100.0|length>0)?'#'~gray100.0:'#f8f9fa' }};
$gray-200:                          {{ (gray200.0|length>0)?'#'~gray200.0:'#e9ecef' }};
$gray-300:                          {{ (gray300.0|length>0)?'#'~gray300.0:'#dee2e6' }};
$gray-400:                          {{ (gray400.0|length>0)?'#'~gray400.0:'#ced4da' }};
$gray-500:                          {{ (gray500.0|length>0)?'#'~gray500.0:'#adb5bd' }};
$gray-600:                          {{ (gray600.0|length>0)?'#'~gray600.0:'#6c757d' }};
$gray-700:                          {{ (gray700.0|length>0)?'#'~gray700.0:'#495057' }};
$gray-800:                          {{ (gray800.0|length>0)?'#'~gray800.0:'#343a40' }};
$gray-900:                          {{ (gray900.0|length>0)?'#'~gray900.0:'#212529' }};
$black:                             {{ (black.0|length>0)?'#'~black.0:'#000' }};
$primary:                           {{ (primary.0|length>0)?'#'~primary.0:'#007bff' }};
$secondary:                         {{ (secondary.0|length>0)?'#'~secondary.0:'#6c757d' }};
$success:                           {{ (success.0|length>0)?'#'~success.0:'#28a745' }};
$info:                              {{ (info.0|length>0)?'#'~info.0:'#17a2b8' }};
$warning:                           {{ (warning.0|length>0)?'#'~warning.0:'#ffc107' }};
$danger:                            {{ (danger.0|length>0)?'#'~danger.0:'#dc3545' }};
$light:                             {{ (light.0|length>0)?'#'~light:'$gray-100'}};
$dark:                              {{ (dark.0|length>0)?'#'~dark:'$gray-800' }};

{% if tertiary.0|length>0 %}
$tertiary:                          {{ tertiary.0 }};
$theme-colors: (
        "tertiary": $tertiary
);
$colors: (
        "tertiary": $tertiary
);
{% endif %}


/* hier geht's los */

$yiq-text-dark:                     {{ (yiqTextDark|length>0?yiqTextDark:'$gray-900') }};
$yiq-text-light:                    {{ (yiqTextLight|length>0?yiqTextLight:'$white') }};

$enable-caret:                      {{ (enableCaret==1)?'true':'false'}};
$enable-rounded:                    {{ enableRounded==1?'true':'false' }};
$enable-shadows:                    {{ enableShadows==1?'true':'false' }};
$enable-gradients:                  {{ enableGradients==1?'true':'false' }};

$spacer:                            {{ spacer|length>0?spacer.value~spacer.unit:'1rem' }};

$body-bg:                           {{ bodyBg|length>0?bodyBg:'$white' }};
$body-color:                        {{ bodyColor|length>0?bodyColor:'$gray-900' }};

$link-color:                        {{ linkColor|length>0?linkColor:'$primary' }};
$link-decoration:                   {{ linkDecoration|length>0?linkDecoration:'none'}};
$link-hover-color:                  {{ linkHoverColor|length>0?linkHoverColor:'darken($link-color, 15%)'}};
$link-hover-decoration:             {{ linkHoverDecoration|length>0?linkHoverDecoration:'underline'}};

$paragraph-margin-bottom:           {{ paragraphMarginBottom|length>0?paragraphMarginBottom:'1rem' }};

$line-height-lg:                    {{ lineHeightLg|length>0?lineHeightLg:1.5 }};
$line-height-sm:                    {{ lineHeightSm|length>0?lineHeightSm:1.5 }};

$border-width:                      {{ borderWidth|length>0?borderWidth:'1px'}};
$border-color:                      {{ borderColor|length>0?borderColor:'$gray-300'}};

$border-radius:                     {{ borderRadius|length>0?borderRadius:'.25rem' }};
$border-radius-sm:                  {{ borderRadiusSm|length>0?borderRadiusSm:'.2rem' }};
$border-radius-lg:                  {{ borderRadiusLg|length>0?borderRadiusLg:'.3rem' }};

$box-shadow:                        {{ boxShadow|length>0?boxShadow: 'none'}};
$box-shadow-sm:                     {{ boxShadowSm|length>0?boxShadowSm: 'none'}};
$box-shadow-lg:                     {{ boxShadowLg|length>0?boxShadowLg: 'none'}};

$component-active-color:            {{ componentActiveColor|length>0?componentActiveColor:'$white'}};
$component-active-bg:               {{ componentActiveBg|length>0?componentActiveBg:'$primary' }};

$font-family-sans-serif:            {{ fontFamilySansSerif|length>0?fontFamilySansSerif:'-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"' }};
$font-size-base:                    {{ fontSizeBase }};
$font-size-lg:                      {{ fontSizeLg|length>0?fontSizeLg: '($font-size-base * 1.25)'}};
$font-size-sm:                      {{ fontSizeSm|length>0?fontSizeSm: '($font-size-base * .875)'}};

$font-weight-normal:                {{ fontWeightNormal|length>0?fontWeightNormal:400 }};
$font-weight-light:                 {{ fontWeightLight|length>0?fontWeightLight:300 }};
$font-weight-bold:                  {{ fontWeightBold|length>0?fontWeightBold:700 }};
$font-weight-base:                  {{ fontWeightBase|length>0?fontWeightBase:'$font-weight-normal' }};

$line-height-base:                  {{ lineHeightBase|length>0?lineHeightBase:1.5 }};

$h1-font-size:                      {{ h1FontSize|length>0?h1FontSize: '$font-size-base * 2.5'}};
$h2-font-size:                      {{ h2FontSize|length>0?h2FontSize: '$font-size-base * 2'}};
$h3-font-size:                      {{ h3FontSize|length>0?h3FontSize: '$font-size-base * 1.75'}};
$h4-font-size:                      {{ h4FontSize|length>0?h4FontSize: '$font-size-base * 1.5'}};
$h5-font-size:                      {{ h5FontSize|length>0?h5FontSize: '$font-size-base * 1.25'}};
$h6-font-size:                      {{ h6FontSize|length>0?h6FontSize: '$font-size-base'}};

$headings-margin-bottom:            {{ headingsMarginBottom|length>0?headingsMarginBottom:'($spacer / 2)'}};
$headings-font-family:              {{ headingsFontFamily|length>0?headingsFontFamily: 'inherit' }};
$headings-font-weight:              {{ headingsFontWeight|length>0?headingsFontWeight:500 }};
$headings-line-height:              {{ headingsLineHeight|length>0?headingsLineHeight:'1.2'}};
$headings-color:                    {{ headingsColor|length>0?headingsColor:'inherit'}};

$lead-font-size:                    {{ leadFontSize|length>0?leadFontSize:'$font-size-base*1.25'}};
$lead-font-weight:                  {{ leadFontWeight|length>0?leadFontWeight: 300 }};

$text-muted:                        {{ textMuted|length>0?textMuted:'$gray-600'}};

$hr-border-color:                   {{ hrBorderColor|length>0?:'rgba($black, .1)'}};
$hr-border-width:                   {{ hrBorderWidth|length>0?hrBorderWidth:'$border-width' }};
$hr-margin-y:                       {{ hrMarginY|length>0?hrMarginY:'$spacer'}};

$list-inline-padding:               {{ listInlinePadding|length>0?listInlinePadding:'.5rem' }};

/*
------------------------------------------------------------
Imports
------------------------------------------------------------
*/
@import "{{ pathToBootstrap }}/scss/functions";
@import "{{ pathToBootstrap }}/scss/variables";
@import "{{ pathToBootstrap }}/scss/mixins";
@import "{{ pathToBootstrap }}/scss/root";
@import "{{ pathToBootstrap }}/scss/reboot";
@import "{{ pathToBootstrap }}/scss/type";
@import "{{ pathToBootstrap }}/scss/images";
@import "{{ pathToBootstrap }}/scss/code";
@import "{{ pathToBootstrap }}/scss/grid";
@import "{{ pathToBootstrap }}/scss/tables";
@import "{{ pathToBootstrap }}/scss/forms";
@import "{{ pathToBootstrap }}/scss/buttons";
@import "{{ pathToBootstrap }}/scss/transitions";
@import "{{ pathToBootstrap }}/scss/dropdown";
@import "{{ pathToBootstrap }}/scss/button-group";
@import "{{ pathToBootstrap }}/scss/input-group";
@import "{{ pathToBootstrap }}/scss/custom-forms";
@import "{{ pathToBootstrap }}/scss/nav";
@import "{{ pathToBootstrap }}/scss/navbar";
@import "{{ pathToBootstrap }}/scss/card";
@import "{{ pathToBootstrap }}/scss/breadcrumb";
@import "{{ pathToBootstrap }}/scss/pagination";
@import "{{ pathToBootstrap }}/scss/badge";
@import "{{ pathToBootstrap }}/scss/jumbotron";
@import "{{ pathToBootstrap }}/scss/alert";
@import "{{ pathToBootstrap }}/scss/progress";
@import "{{ pathToBootstrap }}/scss/media";
@import "{{ pathToBootstrap }}/scss/list-group";
@import "{{ pathToBootstrap }}/scss/close";
@import "{{ pathToBootstrap }}/scss/modal";
@import "{{ pathToBootstrap }}/scss/tooltip";
@import "{{ pathToBootstrap }}/scss/popover";
@import "{{ pathToBootstrap }}/scss/carousel";
@import "{{ pathToBootstrap }}/scss/utilities";
@import "{{ pathToBootstrap }}/scss/print";




/*
------------------------------------------------------------
Additions
------------------------------------------------------------
*/

h1, .h1 {
    color: {{ (h1Color|length>0?h1Color:'$headings-color') }}
}

{% if h1BorderWidth is defined and h1BorderWidth|length>0 %}
h1,.h1 {
    position: relative;
    overflow: hidden;
}
h1:after, .h1:after {
    position: absolute;
    content: '';
    border-bottom: {{ h1BorderWidth }} solid {{ (h1BorderColor|length>0?h1BorderColor:'inherit') }} ;
    margin-left: 0.25em;
    width: 100%;
    bottom: 0.2em;
}
h1.noborder:after, .h1.noborder:after {
    display: none !important;
}
{% endif %}

h2, .h2 {
    color: {{ (h2Color|length>0?h2Color:'$headings-color') }}
}

{% if h2BorderWidth is defined and h2BorderWidth|length>0 %}
h2,.h2 {
    position: relative;
    overflow: hidden;
}
h2:after, .h2:after {
    position: absolute;
    content: '';
    border-bottom: {{ h2BorderWidth }} solid {{ (h2BorderColor|length>0?h2BorderColor:'inherit') }} ;
    margin-left: 0.25em;
    width: 100%;
    bottom: 0.2em;
}
h2.noborder:after, .h2.noborder:after {
    display: none !important;
}
{% endif %}

h3, .h3 {
    color: {{ (h3Color|length>0?h3Color:'$headings-color') }}
}

{% if h3BorderWidth is defined and h3BorderWidth|length>0 %}
h3,.h3 {
    position: relative;
    overflow: hidden;
}
h3:after, .h3:after {
    position: absolute;
    content: '';
    border-bottom: {{ h3BorderWidth }} solid {{ (h3BorderColor|length>0?h3BorderColor:'inherit') }} ;
    margin-left: 0.25em;
    width: 100%;
    bottom: 0.2em;
}
h3.noborder:after, .h3.noborder:after {
    display: none !important;
}
{% endif %}


.lead {
    font-family: {{ (leadFontFamily|length>0?leadFontFamily:'inherit') }};
    color: {{ (leadColor|length>0?leadColor:'inherit') }};
}

.teaser {
    @extend .lead
}

.highlight {
    color: $info;
}
.confirmation  {
    color: $success;
}
.information {
    color: $info;
}
.warning {
    color: $warning;
}
.error {
    color: $danger;
}







